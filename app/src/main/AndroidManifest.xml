<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vinicius.condominium">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity
            android:name=".app.LoginActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.AddEntradaActivity"
            android:label="Adicionar entrada"
            android:parentActivityName=".app.MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".app.AddOcorrenciaActivity"
            android:label="Adicionar ocorrência"
            android:parentActivityName=".app.MainActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".app.MainActivity"></activity>
        <activity android:name=".app.EntradaActivity" android:parentActivityName=".app.MainActivity"></activity>
    </application>

</manifest>